import React from 'react';
import TextInput, { TextInputProps } from '../Formik/TextInput';

const AddressInput = (props: TextInputProps) => {
  return <TextInput {...props} />;
};

export default AddressInput;