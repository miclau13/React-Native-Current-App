import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  buttonContainer: {
    alignSelf: "center",
    borderRadius: 20,
    marginTop: 16,
    width: "75%",
  },
  totalCostText: {
    marginBottom: 8, 
    textAlign: 'center',
  },
});

export default styles;