import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  button: {
    borderRadius: 0, 
    marginBottom: 0,
    marginLeft: 0, 
    marginRight: 0, 
  },
  title: {
    marginBottom: 10,
    textAlign: 'center',
  },
});

export default styles;